# thread-cpucore
this is the simple script, which can be used to assign cpu cores to threads. 
to use this script, we should name individual threads in code.
script takes process name as input and file where mapping is provided with thread names cpu ids
